# AI Text Summarizer CLI

##  Overview
This is a simple command-line tool that takes unstructured text and generates a structured summary using an LLM.

It extracts:
- One-sentence summary
- Three key points
- Sentiment (positive / neutral / negative)

---

##  Setup

```bash
git clone <your-repo-link>
cd ai-text-summarizer-cli
npm install
